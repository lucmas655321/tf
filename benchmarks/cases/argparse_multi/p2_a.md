Still working on `plain.py`. Be concise.

When `ArgumentParser.parse_args()` is called, what is the full internal call sequence?
Walk through each method called in order, stopping when the final namespace is returned.
