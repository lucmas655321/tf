Look at `tf.py` as it stands now with both changes applied. Are `epilog_formatter`
and `format_help_sections` consistent with the existing `ArgumentParser` API in terms
of naming conventions, parameter placement, and docstring style? Any issues?
