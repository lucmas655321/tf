In `plain.py`, add a method `ArgumentParser.format_help_sections()` that returns
a list of `(title, content)` tuples — one per section of the help output — instead
of a single formatted string. Reuse existing formatter machinery. Implement minimally.
Show the unified diff.
