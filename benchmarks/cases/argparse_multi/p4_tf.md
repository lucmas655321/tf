Referring to the `epilog_formatter` change you just made in `tf.py`: trace exactly
where the epilog gets formatted and confirm whether your change is actually reachable
from `ArgumentParser.format_help()`. If not, fix it.
