Look at `tf.py` as it stands now with the `as_dict()` change applied.
Is `as_dict()` consistent with the existing `Decimal` API in terms of
naming conventions, placement, and docstring style? Any issues?
