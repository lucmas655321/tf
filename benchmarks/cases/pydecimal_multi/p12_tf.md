Still working on `tf.py`. Be concise.

In `tf.py`, add `Decimal.to_eng_dict()` returning {'mantissa': str, 'exponent': int}
where exponent is a multiple of 3 (engineering notation). Place after
`to_eng_string`. Show diff.
