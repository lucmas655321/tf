Look at `plain.py` as it stands now with both `as_dict()` and `quantize_to_places()`
applied. Are both new methods consistent with the existing `Decimal` API in terms
of naming conventions, parameter placement, and docstring style? Any issues?
