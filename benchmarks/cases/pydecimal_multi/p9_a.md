Still working on `plain.py`. Be concise.

Look at `plain.py` with `signum()` applied. Is the implementation idiomatic for
the `Decimal` class style (NaN handling, return type, docstring)? Any issues?
