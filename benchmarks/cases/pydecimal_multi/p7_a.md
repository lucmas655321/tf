Still working on `plain.py`. Be concise.

In `plain.py`, list the rounding modes supported by `Decimal._round` (i.e. all
`ROUND_*` strategies it dispatches to). For each, give a one-line description.
