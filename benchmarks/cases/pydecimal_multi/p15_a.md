Still working on `plain.py`. Be concise.

In `plain.py`, add `Decimal.clamp(low, high)` returning self bounded between
low and high (using existing `min`/`max` methods). Place after `max`.
Show diff.
