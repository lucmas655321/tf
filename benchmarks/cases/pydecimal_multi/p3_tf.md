In `tf.py`, add a method `as_dict()` to the `Decimal` class. It should
return a plain dict with keys `sign`, `digits`, and `exponent` — the same
data as `as_tuple()` but as a dict for easier unpacking. Implement minimally,
placing it immediately after the existing `as_tuple` method.
Show the unified diff of your changes.
